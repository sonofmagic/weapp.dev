---
import type { ShaderPreset } from '../lib/shaders'

interface Props {
  preset: ShaderPreset
  label: string
  class?: string
  scale?: number
  interactive?: boolean
}

const { preset, label, class: className = '', scale = 1, interactive = false } = Astro.props
---

<canvas
  class={className}
  data-shader-canvas
  data-shader={preset}
  data-shader-scale={scale}
  data-shader-interactive={interactive ? 'true' : undefined}
  aria-label={label}
  role="img"
></canvas>

<script>
  import { mountShaderCanvases } from '../scripts/shader-canvas'

  mountShaderCanvases()
</script>
